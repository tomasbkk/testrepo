name: Artifact selection
on:
  workflow_dispatch:
    inputs:
      artifact:
        description: "Select an artifact to use"
        required: true
        options: ${{ fromJson(steps.list-artifacts.outputs.artifacts) }}

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Use selected artifact
        run: |
          echo "Selected artifact: ${{ github.event.inputs.artifact }}"
          # Use the selected artifact in your job
